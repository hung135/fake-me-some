 # fill all these out
db:
    connection:
        appname: data_faker #used to tag dbconnection for visibility in db
        db: postgres
        host: pgdb
        port: 5432
        type: 'POSTGRES'
        schema: "test"
        userid: 'docker'
        password_envir_var: PGPASSWORD #set environment variable PGPASSWORD
Tables:
    test12:
        name: person.name #faker provider name from faker_doc.txt
        ssn: ssn.ssn #faker provider
        first_name: ssn.ssn #faker provider
        last_name: ssn.ssn
        phone_number: phone_number.phone_number
        loan_amount: bigint
    company:
        company_name: company.name
        company_address: address.address
        company_size: int
    test_table3:
        color_name: color.color_name
        hex_color: color.hex_color
            
            